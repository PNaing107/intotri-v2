---
import Layout from '../layouts/Layout.astro';
import LeaderboardLink from '../components/intotri/LeaderboardLink.astro';
import { leaderboards, leaderboardTitles } from '../data/leaderboards';

const years = leaderboardTitles.map(({ year, title }) => ({
	year,
	title,
	leaderboards: leaderboards.filter((leaderboard) => leaderboard.year === year),
}));

const defaultYear = years[0]?.year ?? 2026;
const selectedClasses = ['bg-volt', 'text-white'];
const idleClasses = ['bg-transparent', 'text-oxygen'];
---

<Layout title="Leaderboard | IntoTri">
	<main>
		<section id="leaderboard-archive" class="surface-void px-5 pb-20 pt-28 md:px-10 md:pb-28 md:pt-36">
			<div class="mx-auto max-w-[1600px]">
				<div class="velocity-line"></div>

				<div class="py-10 md:py-12">
					<p class="data-mono mb-4 text-[10px] font-500 uppercase tracking-[0.22em] text-oxygen/70 md:text-[11px]">
						/ 01 — Archive
					</p>
					<h1 class="font-archive text-4xl font-900 uppercase leading-[0.86] tracking-tighter2 text-oxygen md:text-6xl">
						Leaderboard
					</h1>
					<p class="mt-6 max-w-2xl font-body text-[15px] leading-[1.6] text-oxygen/80 md:mt-8 md:text-lg">
						Season standings and ranking system PDFs from the Cornish Tri Series. Open a link to view the full PDF.
					</p>
					<div
						class="mt-6 inline-flex w-fit flex-wrap border border-[hsl(var(--oxygen)/0.12)] md:mt-8"
						role="group"
						aria-label="Filter leaderboards by year"
						data-year-filters
					>
						{
							years.map(({ year }) => {
								const selected = year === defaultYear;
								return (
									<button
										type="button"
										class:list={[
											'chrono-hover px-5 py-3 font-archive text-xs font-700 uppercase tracking-[0.18em]',
											selected ? selectedClasses : idleClasses,
										]}
										aria-pressed={selected ? 'true' : 'false'}
										data-filter={year}
										data-cursor="target"
									>
										{year}
									</button>
								);
							})
						}
					</div>
				</div>

				<div class="velocity-line"></div>

				<div class="pt-10 md:pt-12">
					{
						years.map(({ year, title, leaderboards: yearLeaderboards }) => (
							<div
								aria-label={`${year} IntoTri leaderboards`}
								data-year={year}
								hidden={year !== defaultYear}
							>
								<h2 class="font-archive text-xl font-900 uppercase leading-[0.9] tracking-tighter2 text-oxygen md:text-2xl">
									{title}
								</h2>
								<ul class="mt-6 flex flex-col gap-4 md:mt-8">
									{yearLeaderboards.map((leaderboard) => (
										<li>
											<LeaderboardLink leaderboard={leaderboard} />
										</li>
									))}
								</ul>
							</div>
						))
					}
				</div>
			</div>
		</section>
	</main>
</Layout>

<script>
	const section = document.getElementById('leaderboard-archive');
	const buttons = section?.querySelectorAll<HTMLButtonElement>('[data-filter]');
	const grids = section?.querySelectorAll<HTMLElement>('[data-year]');

	const selectedClasses = ['bg-volt', 'text-white'];
	const idleClasses = ['bg-transparent', 'text-oxygen'];

	function setYear(year: string) {
		buttons?.forEach((button) => {
			const selected = button.dataset.filter === year;
			button.setAttribute('aria-pressed', String(selected));
			button.classList.remove(...(selected ? idleClasses : selectedClasses));
			button.classList.add(...(selected ? selectedClasses : idleClasses));
		});

		grids?.forEach((grid) => {
			grid.hidden = grid.dataset.year !== year;
		});
	}

	buttons?.forEach((button) => {
		button.addEventListener('click', () => {
			const year = button.dataset.filter;
			if (year) setYear(year);
		});
	});
</script>
